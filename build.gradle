plugins {
    id 'java-library'
    id 'eclipse'
}

subprojects {
    apply plugin: 'eclipse'

    eclipse {
         project.name = "TinyWasmR - ${project.name}"
    }

    repositories {
         mavenCentral()
    }

    java {
        toolchain {
            languageVersion = JavaLanguageVersion.of(17)
        }
    }
}
